{
  "name": "vesta-chess-web",
  "version": "1.0.0",
  "description": "Browser chess example using Vesta (lobby + invitation + private per-match channels).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "chess.js": "^1.0.0",
    "vesta-client": "file:../../clients/vesta-client-ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
